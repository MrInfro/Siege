name: SiegeMode
main: MrInfroGusko.SiegeMode.SiegeMode
version: 0.1
author: MrInfro, Gusko

commands:
  sm:
    aliases: [siegemode]
    description: SiegeMode Commands
    usage: /sm
    
permissions:
  SiegeMode.use:
     description: Use the plugin.
     default: op